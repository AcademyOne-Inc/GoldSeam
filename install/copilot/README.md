# Microsoft Copilot

GoldSeam is a remote MCP server over streamable HTTP with no authentication, at

```
https://goldseam.ksaworks.com/mcp
```

In Copilot Studio, add it as an MCP tool/connector with that URL and no authentication. In an
organization, this is an administrator's action; the address is the whole configuration.
