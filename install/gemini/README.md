# Google Gemini

GoldSeam is a remote MCP server over streamable HTTP with no authentication, at

```
https://goldseam.ksaworks.com/mcp
```

Add it wherever your Gemini surface accepts a remote MCP server. For the Gemini CLI and other
config-driven clients, the snippet in [`../mcp.json`](../mcp.json) is the whole setup.
